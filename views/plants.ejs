<!DOCTYPE html>
<html lang="en">
  <% include partials/head %>
  <body>
    <% include partials/header %>

    <div class="page-content">
      <div class="container">
        <div class="row content-box">
          <h2>Badass Plants</h2>
          <p>Below are a few of our favorite plants to grow in DC due to their versatility in the kitchen, their awesome beauty, and the depth of benefit they offer.</p>
        </div>
        <% for(var i = 0; i < plants.length; i+=2){%>
          <div class="row">
            <div class="col-lg-6">
              <div class="row content-box">
                <div class="col-sm-5">
                  <img class="about-image" src="/images/<%=plants[i].image%>" />
                </div>
                <div class="col-sm-7">
                  <h4><%= plants[i].name %><% if(plants[i].botanical){ %> <sub>(<%= plants[i].botanical %>)</sub><% } %></h4>

                  <p><%= plants[i].description %></p>
                </div>
              </div>
            </div>
            <% if(i + 1 < plants.length) { %>
            <div class="col-lg-6">
              <div class="row content-box">
                <div class="col-sm-5">
                  <img class="about-image" src="/images/<%=plants[i + 1].image%>" />
                </div>
                <div class="col-sm-7">
                  <h4><%= plants[i + 1].name %><% if(plants[i + 1].botanical){ %> <sub>(<%= plants[i + 1].botanical %>)</sub><% } %></h4>

                  <p><%= plants[i + 1].description %></p>
                </div>
              </div>
            </div>
            <% } %>
          </div>
        <%}%>
      </div>
    </div>

  </body>
  <% include partials/footer %>
</html>
